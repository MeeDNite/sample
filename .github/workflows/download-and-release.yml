name: Download and Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  upload-asset:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          curl -L "https://huggingface.co/OpenMOSS-Team/MOSS-TTS-Nano-100M/resolve/main/pytorch_model.bin?download=true" -o pytorch_model.bin

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          files: pytorch_model.bin
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
